# aws_ec2.yml
plugin: aws_ec2
regions:
  - us-east-1
keyed_groups:
  - key: tags.Environment
    prefix: env
  - key: tags.MongoRole
    prefix: mongo
filters:
  tag:Project: mongodb-cluster
compose:
  ansible_host: public_ip_address